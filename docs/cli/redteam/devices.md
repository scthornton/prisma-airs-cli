# redteam devices

### redteam devices create

Create devices for an instance

```text
airs redteam devices create [options] <tenantId>
```

#### Arguments

- `tenantId` (required) —

#### Options

| Flag | Required | Default | Description |
|------|:--------:|---------|-------------|
| `--config <path>` | Yes | — | JSON file with device request |

#### Examples

!!! warning "Example needed"
    No curated input/output example for this command yet.

---

### redteam devices update

Update devices for an instance (PATCH)

```text
airs redteam devices update [options] <tenantId>
```

#### Arguments

- `tenantId` (required) —

#### Options

| Flag | Required | Default | Description |
|------|:--------:|---------|-------------|
| `--config <path>` | Yes | — | JSON file with device request |

#### Examples

!!! warning "Example needed"
    No curated input/output example for this command yet.

---

### redteam devices delete

Delete devices by serial numbers

```text
airs redteam devices delete [options] <tenantId>
```

#### Arguments

- `tenantId` (required) —

#### Options

| Flag | Required | Default | Description |
|------|:--------:|---------|-------------|
| `--serial-numbers <list>` | Yes | — | Comma-separated serial numbers |

#### Examples

!!! warning "Example needed"
    No curated input/output example for this command yet.
